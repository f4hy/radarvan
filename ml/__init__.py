"""Radarvan match-outcome ML pipeline.

All training / dataset-curation / model code lives here. Dependencies are in the
non-default ``ml`` dependency-group (``uv sync --group ml``) and never ship to
production. See ``ml/model_design.md`` for the architecture and rationale.
"""
